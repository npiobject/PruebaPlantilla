# Sesión 2026-09-06 09:25 — inicialización de PruebaPlantilla desde la plantilla

1. **Qué cambió:** sustituidos los marcadores de la plantilla en 12 ficheros (`app/fly.toml` y `FLY_APP` → `pruebaplantilla-npi`, paquete Rust → `pruebaplantilla-backend` en `Cargo.toml`/`Cargo.lock`/`Dockerfile`, textos de `main.rs` y del mock, defaults de `tools/*.ps1`, y `CLAUDE.md` y `README.md` con owner, URLs vivas e id de Drive); añadido `docs/planificacion/inicializacion-desde-plantilla.md` con las diez fricciones detectadas en `ARRANQUE.md`.
2. **SHA:** `7ee507a3cf2fd4ff009a9abd41ec79430d751a60` (runs [34024493221](https://github.com/npiobject/PruebaPlantilla/actions/runs/34024493221) Pages y [34024493213](https://github.com/npiobject/PruebaPlantilla/actions/runs/34024493213) Fly, ambos `success`).
3. **URLs para probar:** https://npiobject.github.io/PruebaPlantilla/ · https://pruebaplantilla-npi.fly.dev/ · https://pruebaplantilla-npi.fly.dev/salud → `{"build":"7ee507a3…","ok":true}` (build `PP-B1-20260906-001` en el mock).
4. **Drive:** copia de este resumen en `sesiones/` y del documento de inicialización en la raíz de `Mi unidad/PruebaPlantilla` (id `1EUL0hBVLSHsUWz9auVSXF7J-Z90hNqXl`), sin borrar nada.
5. **Qué falta:** decidir sobre las fricciones 1–4 y 8 del documento (lista incompleta de ficheros en `ARRANQUE.md`, prefijo de build parametrizable, destino de la documentación heredada, y push a `main` explícito en el bloque a pegar); los `tools/*.ps1` siguen sin probarse por falta de `pwsh` en el sandbox.
